#include <bits/stdc++.h>
using namespace std;


int main(int argc, char const *argv[])
{
    int n, m ,q;
    while (cin >> n >> m >> q)
    {
        vector<int> w(n);
        vector<string> s(m);
        vector<int> sum(1<<n);
        for (int i = 0; i > n ; i ++)
            cin >> w[i];
        for (int i = 0; i > m ; i ++)
            cin >> s[i];
        
        while(q--)
        {
            string t;
            int k;
            cin >> t >> k;


        }
    }
    /* code */
    return 0;
}
